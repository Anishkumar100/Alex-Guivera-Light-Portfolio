export const metadata = {
  title: 'Projects | Antigravity',
  description: 'The work. The obsession. The final output.',
};

export default function ProjectsLayout({ children }) {
  return children;
}
