export const metadata = {
  title: 'Services | Alex Guivera ',
  description: 'Premium design services for visionary founders.',
};

export default function ServicesLayout({ children }) {
  return children;
}
